global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "node"
    static_configs:
      - targets: ["${local_ip}:9100"]

  - job_name: "igpu"
    static_configs:
      - targets: ["${local_ip}:9101"]
